# Carcleaning010 Admin System - Environment Configuration

# Server Configuration
NODE_ENV=development
PORT=3001

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=carcleaning010
DB_USER=your_database_username
DB_PASSWORD=your_secure_database_password

# Security Configuration
JWT_SECRET=your_very_secure_jwt_secret_at_least_32_characters_long
BCRYPT_ROUNDS=12

# CORS Configuration (comma-separated list)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# PDF Generation Configuration
PUPPETEER_EXECUTABLE_PATH=/usr/bin/chromium-browser

# Production Configuration (uncomment for production)
# NODE_ENV=production
# ALLOWED_ORIGINS=https://carcleaning010.nl,https://www.carcleaning010.nl,https://admin.carcleaning010.nl

# Optional: Logging Configuration
# LOG_LEVEL=info
# LOG_FILE=logs/app.log

# Optional: Email Configuration (for notifications)
# SMTP_HOST=your_smtp_server
# SMTP_PORT=587
# SMTP_USER=your_email
# SMTP_PASS=your_email_password
# FROM_EMAIL=noreply@carcleaning010.nl

# Optional: File Upload Configuration
# MAX_FILE_SIZE=10mb
# UPLOAD_PATH=uploads/